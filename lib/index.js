'use strict';

const ImageManipulation = require('../services/image-manipulation')
const Upload = require('../services/upload')

const services = {
    ImageManipulation,
    Upload
}

module.exports = {
    services
}